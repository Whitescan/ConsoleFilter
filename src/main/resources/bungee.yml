name: ${project.name}
version: ${project.version}
description: ${project.description}
author: Whitescan
api-version: 1.18
load: STARTUP
main: me.whitescan.consolefilter.waterfall.WaterfallConsoleFilter

commands:
    breloadconsolefilter:
      description: Reload the console filter
      permission: consolefilter.reload
